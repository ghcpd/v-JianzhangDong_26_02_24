## Tests failing, need CI/CD fix

Running `python run_ci.py` fails. Pretty sure it's the dependency versions (Pydantic looks wrong) and maybe pytest config too.

Can you fix this? Copy everything to a new `issue_fixed` folder and work there. Need you to:
- Fix requirements.txt with correct compatible versions
- Create clean .venv/ virtual environment 
- Update .gitignore for .venv/
- Run all tests and save results to logs/test_run.log
- Generate report.json with what you fixed

Don't touch the test files themselves - they should pass once dependencies are right. All tests must pass when done.